import React, {useState} from 'react';
import axios from 'axios';

function DeleteResource({token}) {
    

    return (
        <div className="resources section">
            
        </div>
      );
}

export default DeleteResource;